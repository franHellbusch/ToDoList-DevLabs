import { Button, styled } from "@mui/material";

export const LoginButton = styled(Button)(() => ({
  display: "flex",
  alignItems: "center",
  gap: 5,
  textTransform: "none",
  fontWeight: 600,
  fontSize: 16,
  marginTop: 40,
}));
