import { Button, styled } from "@mui/material";

export const AddTaskButton = styled(Button)(() => ({
  display: "flex",
  alignItems: "center",
  gap: 5,
  textTransform: "none",
  fontWeight: 600,
}));
