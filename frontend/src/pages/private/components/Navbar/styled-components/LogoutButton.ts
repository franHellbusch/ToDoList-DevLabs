import { Button, styled } from "@mui/material";

export const LogoutButton = styled(Button)(() => ({
  display: "flex",
  alignItems: "center",
  gap: 5,
  textTransform: "none",
  fontWeight: 600,
}));
